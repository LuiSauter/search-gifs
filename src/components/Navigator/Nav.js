import styled from 'styled-components'

const Nav = styled.nav`
  text-align: center;
  /* padding: 0.5em; */
  margin: 1.7rem 0;
  img {
    cursor: pointer;
    width: 100px;
  }

`
export default Nav
